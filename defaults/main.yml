---
# defaults file for midonet-repo
midonet_base_url: "http://builds.midonet.org"
midonet_version: "current"
midonet_release: "stable"
openstack_version: "mitaka"
plugin_release: "stable"

# TODO Hardcoded values for MEM. Update it later.
mem_username: "midonet"
mem_password: "*******"
mem_base_url: "http://{{ mem_username }}:{{ mem_password }}@builds.midonet.org/mem-5.2"
